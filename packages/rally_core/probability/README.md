# Probability

Probability engine mencari waypoint paling mungkin ketika data tidak lengkap.

Input:

```text
previous waypoint
missing waypoint token
next waypoint
time/distance target
navigation action
candidate pool
```

Output:

```text
selected candidate
confidence
alternatives
explanation
decision status
```

